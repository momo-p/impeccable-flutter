name: impeccable_flutter
description: Deterministic design detector for Flutter source, ported from pbakaus/impeccable.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
