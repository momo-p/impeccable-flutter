/// The little bit of domain the screens read from.
class Entry {
  const Entry({required this.title, required this.note, required this.poster});

  final String title;
  final String note;
  final String poster;
}
