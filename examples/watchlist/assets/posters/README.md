# Posters

Two-pixel flat-colour placeholders, so the assets declared in `pubspec.yaml`
resolve and the example runs without shipping artwork nobody licensed.

`library_after.dart` also handles a failed load with `errorBuilder`, so
replacing these with real images, or deleting them, both render.
