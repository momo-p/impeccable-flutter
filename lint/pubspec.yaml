name: impeccable_flutter_lint
description: Surfaces impeccable-flutter findings in the IDE through custom_lint.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  analyzer: ^7.0.0
  custom_lint_builder: ^0.7.0
  impeccable_flutter:
    path: ../detector

dev_dependencies:
  test: ^1.25.0
  custom_lint: ^0.7.0
  lints: ^5.0.0
